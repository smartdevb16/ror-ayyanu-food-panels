$(".delivery-company-district-list, .delivery-company-zone-list").html("").addClass("hide")

<% if @districts.present? %>
  $(".delivery-company-district-list").removeClass("hide");
  $(".delivery-company-district-list").html('<%= escape_javascript select_tag(:district, options_for_select(@districts), class: "form-control delivery-company-district-select select2_district", style: "height: 34px", multiple: true) %>')
<% end %>

$(".select2_district").select2();