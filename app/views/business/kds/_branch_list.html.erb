<%= select_tag(:branch_ids, options_from_collection_for_select(@branches, :id, :address), {id: "kds_branch_ids", class: 'form-control setheight', style: 'height: 40px!important;', multiple: true, required: true, name: 'kds[branch_ids][]'}) %>
<script type="text/javascript">
	$("#kds_branch_ids").select2({
  	placeholder: "Select Branch",
  	allowClear: true
	});
	
</script>